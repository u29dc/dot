#!/usr/bin/env bash
tools_home="${TOOLS_HOME:-$HOME/.tools}"
tool="${FIN:-$tools_home/fin/fin}"
if [ ! -x "$tool" ]; then
    printf 'fin binary not found: %s\nSet FIN or install it under TOOLS_HOME.\n' "$tool" >&2
    exit 127
fi
exec "$tool" "$@"
